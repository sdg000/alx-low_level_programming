#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char a = 'a';
	int n = 26;

	while (a < 26)
	{
	putchar(a);
	a++;
	n++;
	}
	putchar('\n');
	return (0);
}
