#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char alphas;

	for (alphas = 'a'; alphas <= 'z'; alphas++)
	{
	putchar(alphas);
	}
	putchar('\n');
	return (0);
}
